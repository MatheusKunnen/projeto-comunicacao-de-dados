import { createTheme } from '@material-ui/core/styles';

const mainTheme = createTheme({
  palette: {
    // primary: {
    //   main: purple[500],
    // },
    // secondary: {
    //   main: green[500],
    // },
  },
});

export default mainTheme;
