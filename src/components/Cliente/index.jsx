import React, { Fragment } from 'react';
import Mensagens from '../Mensagem/Mensagens';

const Cliente = () => {
  return (
    <Fragment>
      <Mensagens />
    </Fragment>
  );
};

export default Cliente;
